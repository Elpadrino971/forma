import { ChallengeApp } from "@/components/defi/ChallengeApp";

/**
 * Application du defi 90 jours. Tout se passe cote navigateur : la
 * progression et le journal sont stockes sur l'appareil de l'utilisateur.
 */
export default function DefiPage() {
  return <ChallengeApp />;
}
